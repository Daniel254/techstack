import styled from "styled-components";
import {
  border,
  color,
  flexbox,
  layout,
  space,
  typography,
} from "styled-system";

const Box = styled.div`
  ${space}
  ${layout}
  ${typography}
  ${color}
  ${flexbox}
  ${border}
`;

export default Box;
